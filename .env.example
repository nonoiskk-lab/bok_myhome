DATABASE_URL="postgresql://user:password@host:5432/postgres?sslmode=require"
JWT_SECRET="change-me-in-production"
# Powers the AI chat assistant (src/app/api/chat/route.ts). Get one at
# https://console.anthropic.com/settings/keys — without it the widget still
# renders but replies with a "not activated yet" message instead of erroring.
ANTHROPIC_API_KEY=""
